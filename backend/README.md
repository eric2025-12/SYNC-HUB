Backend Quick Start:

1. Create virtualenv:
   python -m venv venv
   source venv/bin/activate

2. Install:
   pip install -r requirements.txt

3. Configure .env (copy .env.example -> .env) and set DATABASE_URL to your postgres DB

4. Run:
   python run.py

