# Frontend .env

# Base URL of your backend API
VITE_API_BASE=http://localhost:5000/api

# Optional: CORS allowed origins (if needed, default * is usually fine)
VITE_CORS_ORIGINS=*

# Optional: Enable debug logging in frontend
VITE_DEBUG=true
